{
  "name": "typescript-exchange-example",
  "version": "1.0.0",
  "description": "working typescript",
  "main": "index.js",
  "scripts": {
    "compile": "tsc",
    "watch": "tsc -w",
    "server": "lite-server --baseDir=app",
    "json-server": "json-server --watch db.json --port 4041",
    "start": "concurrently \"npm run watch\" \"npm run json-server\" \"npm run server\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jquery": "^2.0.56",
    "concurrently": "^6.0.0",
    "json-server": "^0.16.3",
    "lite-server": "^2.6.1",
    "typescript": "^2.9.2"
  }
}
